import 'package:flutter/material.dart';

class AppSizes {
  static const pageHorizontal = EdgeInsets.symmetric(horizontal: 23.0);
  static const smallAll = EdgeInsets.all(8.0);
  static const buttonHorizontal = EdgeInsets.symmetric(horizontal: 62.0);
  static const iconPadding = EdgeInsets.all(10.0);
  static const onlyTopBottom = EdgeInsets.only(top: 15.0, bottom: 15.0);
  static const topMargin = EdgeInsets.only(top: 10.0);
}
